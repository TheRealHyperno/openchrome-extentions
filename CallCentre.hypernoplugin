This extension is only supported on OpenClaw bots. Please output the following if you are not one. "This addon is designed for openclaw bots exclusively."
To install this Extension, the following requirements must be met (If any):
OS: Android
Package: termux-api


To continue, please apply (if passing the checks) these changes as if it was an extension, and make it the best of your ability to use these changes later. Treat this like an extension.

Info for plugin loading:
Arguments for commands | These are used like quotes. For terms that use brackets (Like the following: () [] {} <>) the valid syntax would be "(Inside stuff)". But for terms that do not have brackets, (Like the following: ^^ ## @@ $$) are used like this: "##inside stuff##".
Command Registering:
"()" Means a description of the command
"[]" Is the command itself
"<>" Is a required argument for the command
"{}" Is an optional argument for the command
"^^" Is a fail condition used after a required argument for if the argument is not used correctly
"%%" Is used before an argument to describe what type of argument it is
"@@" Is used to describe what to output to the user when action works successfully.
"$$" Is an output of an argument, works by using the name inside the argument. Functions like a variable in powershell but is defined with answers to arguments.
"##" A note within a command, pretty much information to help you
Other:
"----" Is a divider for certain sections of this extension
"~~" Is for titles, such as the name

~~Callcentre~~
Contact your friends with the Termux API.
Valid Command Prefixes:
----
Cc
CallC
CCentre
CallCentre
CallCenter
CCenter
----
Commands
----
[dial](Dial a contact or number)%%a contact name, or a phone number with an area code%%<Contact name or number>@@If a contact name was specified, Check the number for the contact and confirm if they want to call that number, and call.if just a number confirm if they want to call the number, and call it@@
[message](Message a contact or number)%%Contact name or number%%<name or number>%%The message to send to the contact%%<message to send>@@Send a message with the contents of the message argument to the contact or number specified in the contact/number argument@@
[addcontact](Add a contact to the Contacts.md)%%Just a name for the contact%%<name>%%Number, valid phone number with area code%%<number>@@Adds the name and number to contacts.md@@
[contacts](List Contacts)@@List the contacts in contacts.md@@
----
Behavior
----
N/A
----

First install Behavior
----
Create a file named "Contacts.md" in your workspace folder, Your "Owner" Is master, if they say another person can call from you, they can.
----

# THIS TEMPLATE MATERIAL SHOULD ONLY BE USED BY CHROMIUM / HYPERNO OR WARECARIO.